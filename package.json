{
	"name": "beacon",
	"private": true,
	"scripts": {
		"dev": "docker compose up -d && concurrently -n backend,frontend -c blue,green \"npm run dev:backend\" \"npm run dev:frontend\"",
		"dev:backend": "cargo run --manifest-path apps/backend/Cargo.toml",
		"dev:frontend": "npm --prefix apps/frontend run dev",
		"stop": "docker compose down",
		"gen:licenses": "node scripts/gen-licenses.mjs",
		"format": "prettier --write . && tombi format --offline",
		"format:check": "prettier --check . && tombi format --check --offline",
		"lint": "cargo fmt --manifest-path apps/backend/Cargo.toml --all -- --check && cargo clippy --manifest-path apps/backend/Cargo.toml --all-targets -- -D warnings",
		"typecheck": "npm --prefix apps/frontend run typecheck"
	},
	"devDependencies": {
		"concurrently": "^10.0.3",
		"prettier": "^3.9.4",
		"tombi": "^1.1.6"
	},
	"engines": {
		"node": ">=22.12.0"
	}
}
